class Advertisement {
  final int id;
  final String title;
  final String description;
  final String imageUrl; // For now, we might use local assets or colors

  Advertisement({
    required this.id,
    required this.title,
    required this.description,
    required this.imageUrl,
  });
}
